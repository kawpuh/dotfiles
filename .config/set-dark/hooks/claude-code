#!/bin/bash
# Claude Code theme hook for set-dark

case "$1" in
    "dark")
        claude config set -g theme dark
        ;;
    "light")
        claude config set -g theme light
        ;;
    "default")
        claude config set -g theme light
        ;;
esac
