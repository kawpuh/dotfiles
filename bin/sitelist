#!/bin/bash

CONFIG_FILE="$HOME/.config/sitelist"

if [[ ! -f "$CONFIG_FILE" ]]; then
    echo "Config file not found: $CONFIG_FILE"
    exit 1
fi

selection=$(cat "$CONFIG_FILE" | rofi -dmenu -p "Select site:")

if [[ -n "$selection" ]]; then
    xdg-open "https://$selection"
fi
