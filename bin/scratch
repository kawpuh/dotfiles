#!/bin/bash
name=tmp-notes/tmp
if [[ ! -d tmp-notes ]] ; then
    mkdir tmp-notes
fi
if [[ -e $name.md ]] ; then
    i=0
    while [[ -e $name-$i.md ]] ; do
        let i++
    done
    name=$name-$i
fi
kitty -o font_size=20 nvim $name.md
