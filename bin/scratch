#!/bin/bash
note_dir=~/Sync/tmp-notes/
if [[ ! -d $note_dir ]] ; then
    mkdir note_dir
fi
cd $note_dir
name=tmp
if [[ -e $name.md ]] ; then
    i=0
    while [[ -e $name-$i.md ]] ; do
        let i++
    done
    name=$name-$i
fi
kitty -o font_size=20 nvim $name.md
